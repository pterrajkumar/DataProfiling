/* Interface for handling upload files */

export interface IProfileInterfaceContext {
    UploadFiles: any[];
}

/* export enum OperationTypeEnum {
    Login = 0,
    Insert = 1,
    Update = 2,
    Delete = 3,
    View = 4,
    Activate = 5,
    Deactivate = 6,
    Lock = 7,
    Unlock = 8,
    Cancel = 9,
    Select = 10
} */
